import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const AppBar = () => {
  return (
    <View>
      <Text>This is app bar</Text>
    </View>
  );
};

export default AppBar;

const styles = StyleSheet.create({});
